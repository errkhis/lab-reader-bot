# Lab Reader Bot

Telegram bot for the Lab Reader API.

## Setup

1. Copy `.env.example` to `.env` and fill in your details.
2. Install dependencies:
   ```bash
   uv sync
   ```
3. Run the bot:
   ```bash
   uv run bot.py
   ```
